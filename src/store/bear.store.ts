// import { create } from "zustand"

// export const useBearStore = create((set) => ({
//   bears: 0,
//   increase: () => set((s) => ({ bears: s.bears + 1 })),
//   reset: () => set({ bears: 0 }),
// }))
